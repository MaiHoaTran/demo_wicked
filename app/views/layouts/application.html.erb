<!DOCTYPE html>
<html>
  <head>
    <title>DemoWicked</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <% if current_user.present? %>
      <h5>Xin chào, <%= link_to "#{current_user.email}", destroy_user_session_path, method: :delete %></h5>
    <% end %>
    <div style="text-align: center; margin-top: 50px" class="row">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>" style="color: red"><%= message %></div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
